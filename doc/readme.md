# run server:
```shell
$ cd build/server
$ docker build . -t pow-server
$ docker run  pow-server

```

#run client:
```shell
$ cd build/client
$ docker build . -t pow-client
$ docker run  pow-client

```